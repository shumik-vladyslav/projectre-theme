import { SearchRulesPipe } from './search-rules.pipe';

describe('SearchRulesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRulesPipe();
    expect(pipe).toBeTruthy();
  });
});
